name: vow
version: 0.3.0
description: Generate a typed client (TypeScript, or JavaScript plus a .d.ts) from your annotated Crystal methods.

authors:
  - Aristotelis Rapai <aristorap.dev@gmail.com>

crystal: ">= 1.20.2"

license: MIT

targets:
  vow:
    main: src/vow_cli.cr

dependencies:
  argy:
    github: AristoRap/argy
